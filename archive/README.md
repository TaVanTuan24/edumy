# Archive

This directory stores non-runtime artifacts and cleanup candidates that were removed from the active project tree during safe refactors.

## Contents

- `editor-cache/jass-cache/`: VS Code extension cache files moved out of `.vscode`.
- `candidates/README.md`: files that look unused but were not moved because runtime safety was not proven.
