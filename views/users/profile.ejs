<% layout('/layouts/boilerplate') %>
<link rel="stylesheet" href="/stylesheets/profile.css">

<%
  const avatarUrl = user && user.avatar && user.avatar.url ? user.avatar.url : '';
  const avatarLetter = (user.username || 'U').charAt(0).toUpperCase();
  const recentBadges = gamification.earnedBadges.slice(-3).reverse();
%>

<div class="profile-page container py-4">
  <div class="profile-header-card">
    <div class="profile-avatar-wrap">
      <% if (avatarUrl) { %>
        <img class="profile-avatar-image" src="<%= avatarUrl %>" alt="Ảnh đại diện của <%= user.username %>">
      <% } else { %>
        <div class="profile-avatar" aria-hidden="true"><%= avatarLetter %></div>
      <% } %>

      <form class="profile-avatar-form" action="/profile/avatar" method="POST" enctype="multipart/form-data">
        <label class="avatar-upload-btn" for="profileAvatarInput">Đổi ảnh đại diện</label>
        <input id="profileAvatarInput" class="avatar-upload-input" type="file" name="avatar" accept="image/png,image/jpeg,image/jpg">
        <button id="profileAvatarSubmit" class="btn btn-primary btn-sm" type="submit" disabled>Cập nhật ảnh</button>
      </form>
    </div>

    <div class="profile-header-copy">
      <h1 class="profile-name"><%= user.username %></h1>
      <p class="profile-email mb-1"><%= user.email %></p>
      <p class="profile-meta mb-0">Tham gia từ <%= new Date(user.createdAt || user._id.getTimestamp()).toLocaleDateString('vi-VN') %></p>
      <div class="profile-actions mt-3">
        <button
          type="button"
          class="btn btn-primary profile-vr-button"
          data-bs-toggle="modal"
          data-bs-target="#vrPairingModal"
        >
          Login VR
        </button>
      </div>

      <% if (recentBadges.length) { %>
        <div class="profile-highlight-badges">
          <% recentBadges.forEach((badge) => { %>
            <span class="profile-highlight-badge"><i class="fa-solid <%= badge.icon %>"></i> <%= badge.name %></span>
          <% }) %>
        </div>
      <% } %>
    </div>
  </div>

  <section class="profile-grid mt-4">
    <article class="profile-card">
      <h2>Tiến độ cấp độ</h2>
      <div class="level-row">
        <span class="level-pill">Cấp <%= gamification.currentLevel %></span>
        <span class="xp-pill"><%= gamification.totalXP %> XP</span>
      </div>
      <div class="xp-track" role="progressbar" aria-valuemin="0" aria-valuemax="100" aria-valuenow="<%= gamification.levelProgress.progressPercent %>">
        <div id="xpProgressFill" class="xp-fill" data-progress="<%= gamification.levelProgress.progressPercent %>"></div>
      </div>
      <p class="progress-note mb-0">
        <% if (gamification.levelProgress.nextLevel) { %>
          Còn <strong><%= gamification.levelProgress.xpToNextLevel %> XP</strong> để lên cấp <%= gamification.levelProgress.nextLevel %>
        <% } else { %>
          Bạn đã đạt cấp tối đa hiện tại
        <% } %>
      </p>
    </article>

    <article class="profile-card">
      <h2>Chuỗi học tập</h2>
      <div class="streak-row">
        <div class="streak-box">
          <i class="fa-solid fa-fire"></i>
          <div>
            <div class="streak-value"><%= gamification.currentStreak %> ngày</div>
            <div class="streak-label">Chuỗi hiện tại</div>
          </div>
        </div>
        <div class="streak-box muted">
          <i class="fa-solid fa-fire-flame-curved"></i>
          <div>
            <div class="streak-value"><%= gamification.longestStreak %> ngày</div>
            <div class="streak-label">Chuỗi dài nhất</div>
          </div>
        </div>
      </div>
      <p class="progress-note mb-0">
        <% if (gamification.lastActivityDate) { %>
          Last activity: <strong><%= new Date(gamification.lastActivityDate).toLocaleDateString('vi-VN', { timeZone: gamification.activityTimezone || 'UTC' }) %></strong>
        <% } else { %>
          Complete your first learning session to start a streak.
        <% } %>
      </p>
    </article>

    <article class="profile-card">
      <h2>Thống kê học tập</h2>
      <ul class="stat-list">
        <li><span>Tổng XP</span><strong><%= gamification.totalXP %></strong></li>
        <li><span>Bài học đã hoàn thành</span><strong><%= gamification.completedLessonsCount %></strong></li>
        <li><span>Quiz đã hoàn thành</span><strong><%= user.gamification && user.gamification.stats ? user.gamification.stats.quizzesCompleted : 0 %></strong></li>
        <li><span>Tương tác AI</span><strong><%= user.gamification && user.gamification.stats ? user.gamification.stats.aiInteractions : 0 %></strong></li>
        <li><span>Khóa học đã hoàn thành</span><strong><%= user.gamification && user.gamification.stats ? user.gamification.stats.coursesCompleted : 0 %></strong></li>
      </ul>
    </article>
  </section>

  <section class="profile-card mt-4">
    <div class="badge-head">
      <div>
        <h2 class="mb-0">Bộ sưu tập huy hiệu</h2>
        <p class="badge-subtitle mb-0">Mở khóa thêm huy hiệu bằng cách học đều, làm quiz và dùng các công cụ AI.</p>
      </div>
      <span class="badge-count"><%= gamification.earnedBadges.length %> / <%= gamification.badges.length %> đã mở khóa</span>
    </div>

    <div class="badge-grid mt-3">
      <% gamification.badges.forEach((badge) => { %>
        <article class="badge-item <%= badge.earned ? 'earned' : 'locked' %>">
          <div class="badge-icon"><i class="fa-solid <%= badge.icon %>"></i></div>
          <div>
            <h3><%= badge.name %></h3>
            <p><%= badge.description %></p>
            <% if (badge.earnedAt) { %>
              <small>Mở khóa ngày <%= new Date(badge.earnedAt).toLocaleDateString('vi-VN') %></small>
            <% } else { %>
              <small>Chưa mở khóa</small>
            <% } %>
          </div>
        </article>
      <% }) %>
    </div>
  </section>

  <section class="profile-card mt-4">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2">
      <div>
        <h2 class="mb-1">Bảng xếp hạng</h2>
        <p class="mb-0 text-muted">Xem vị trí của bạn so với những học viên khác.</p>
      </div>
      <a href="/leaderboard" class="btn btn-primary">Mở leaderboard</a>
    </div>

    <% if (leaderboard && leaderboard.topUsers && leaderboard.topUsers.length > 0) { %>
      <div class="table-responsive mt-3">
        <table class="table table-sm align-middle mb-0">
          <thead>
            <tr>
              <th>Hạng</th>
              <th>Học viên</th>
              <th>Cấp</th>
              <th>XP</th>
            </tr>
          </thead>
          <tbody>
            <% leaderboard.topUsers.forEach((entry) => { %>
              <tr class="<%= entry.isCurrentUser ? 'table-primary' : '' %>">
                <td>#<%= entry.rank %></td>
                <td><%= entry.username %> <%= entry.isCurrentUser ? '(Bạn)' : '' %></td>
                <td><%= entry.level %></td>
                <td><strong><%= entry.totalXP %></strong></td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    <% } %>
  </section>
</div>

<div class="modal fade profile-vr-modal" id="vrPairingModal" tabindex="-1" aria-labelledby="vrPairingModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content profile-modal-shell">
      <div class="modal-header">
        <h5 class="modal-title" id="vrPairingModalLabel">Pair VR Device</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <form id="vrPairingForm">
        <div class="modal-body">
          <label class="form-label fw-semibold" for="vrPairingCodeInput">5-digit pairing PIN</label>
          <input
            id="vrPairingCodeInput"
            class="form-control form-control-lg text-center profile-vr-code-input"
            type="text"
            inputmode="numeric"
            autocomplete="one-time-code"
            maxlength="5"
            pattern="[0-9]{5}"
            placeholder="00000"
          >
          <p class="text-muted small mb-0 mt-2">Open the VR app, request a login code, then enter it here.</p>
          <div id="vrPairingMessage" class="alert d-none mt-3 mb-0" role="alert"></div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancel</button>
          <button type="submit" id="vrPairingSubmit" class="btn btn-primary" disabled>Confirm</button>
        </div>
      </form>
    </div>
  </div>
</div>

<script src="/javascripts/profile.js" defer></script>
